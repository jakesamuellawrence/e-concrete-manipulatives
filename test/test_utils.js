import { expect } from "chai";
var assert = require('assert');
var Utils = require('../source/js/utils.js');


describe("Utils", function() {
    describe("#constructLink()", function() {
        it("should pass this default test.", function() {
            expect(1).to.equal(1);
        });
    });
});

describe("StickSpawner", function() {
    describe ("#spawn()", function() {
        it("", function() {
            expect(0).to.equal(0);
        });
    });
});